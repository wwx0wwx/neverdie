AGENT_PORT=43110
AGENT_NAME=local-agent
AGENT_REGION_CODE=local-dev
AGENT_REGION_NAME=Local Development
AGENT_CITY=
AGENT_COUNTRY=
STALE_AFTER_MS=15000
LOG_LEVEL=info
# network stats mode: single (default) or aggregated (sum all non-loopback NICs)
NETWORK_MODE=single
# only used when NETWORK_MODE=single; leave blank to auto-pick first NIC
PRIMARY_INTERFACE=
# CORS origin restriction (leave blank to allow any origin)
CORS_ORIGIN=
